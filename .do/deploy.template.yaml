spec:
  name: GraphDB
  services:
    - name: sample-gdb
      environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/estebanrfp/git-guide.git
      build_command: npm install
      run_command: npm start
      http_port: 3000
      instance_count: 1
      instance_size_slug: apps-s-1vcpu-0.5gb
      routes:
        - path: /
      envs:
        - key: GRAPHDB_ROOM
          value: todoList
          scope: RUN_TIME
